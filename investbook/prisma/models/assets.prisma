generator client {
  provider = "prisma-client-py"
  output = "../clients/assets"
  previewFeatures = ["views"]
}

datasource db {
  provider = "postgresql"
  url      = env("INVESTBOOK_ASSETS_URL")
}
